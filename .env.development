# Tracked dev defaults — loaded by `pnpm start` and `pnpm dev`.
# Isolates this checkout from a globally-installed `asbox` so the two can
# run side-by-side. Personal overrides go in .env.local (gitignored).

AURICA_HOME=./.aurica-dev
AURICA_PROXY_PORT=51218
